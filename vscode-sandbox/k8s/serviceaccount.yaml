apiVersion: v1
kind: ServiceAccount
metadata:
  name: vscode-sandbox
  namespace: vscode-sandbox
automountServiceAccountToken: false
---
# No Role/RoleBinding: this pod does not access the Kubernetes API.
# Add a Role + RoleBinding here if you ever need to read Secrets, ConfigMaps, etc.
