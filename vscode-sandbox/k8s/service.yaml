apiVersion: v1
kind: Service
metadata:
  name: vscode-sandbox-ssh
  namespace: vscode-sandbox
  annotations:
    metallb.universe.tf/address-pool: default
spec:
  type: LoadBalancer
  selector:
    app: vscode-sandbox
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      protocol: TCP
